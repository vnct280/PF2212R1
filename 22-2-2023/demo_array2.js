// duyệt qua từng phần tử trong mảng
let numbers =[1, 2, 3, 4, 5];

//  vòng lặp  for-in
for (let item  in numbers) {
    document.write(numbers[item] + "<br>");
}
// sử dụng câu lệnh for để duyệt mảng
for(let i = 0; i < numbers.length; i++){
    document.write(numbers[i]+"<br>")
}